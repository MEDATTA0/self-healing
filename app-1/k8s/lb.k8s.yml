apiVersion: v1
kind: Service
metadata:
  name: my-app-1
spec:
  type: LoadBalancer
  selector:
    app: my-app-1
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      nodePort: 31000
